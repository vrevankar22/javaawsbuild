version: 0.2

phases:
  install:
    commands:
      - rm -f /usr/local/bin/chromedriver || true
      - java -version
      - mvn -version

  build:
    commands:
      - mvn clean install
      - mvn exec:java -Dexec.mainClass=OpenAmazon